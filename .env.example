# Variáveis de Ambiente - Weather Insights API

# OpenWeatherMap API Configuration
# Obtenha sua chave gratuita em: https://openweathermap.org/api
OPENWEATHER_API_KEY=your_api_key_here

# OpenWeatherMap Base URL
# URL base da API do OpenWeatherMap (não alterar a menos que use versão diferente)
OPENWEATHER_BASE_URL=https://api.openweathermap.org/data/2.5

# Database Configuration
# String de conexão PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/weather_insights"

# Redis Configuration
# URL de conexão do Redis para cache
# Formato: redis://HOST:PORT ou redis://USER:PASSWORD@HOST:PORT
REDIS_URL="redis://localhost:6379"

# Application Configuration
# Porta onde a aplicação será executada
PORT=3000

# Node Environment
# Valores possíveis: development, production, test
NODE_ENV=development

# JWT Configuration
# Chave secreta para assinar tokens JWT (use uma chave forte em produção)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Tempo de expiração do token JWT
# Exemplos: 1h, 24h, 7d, 30d
JWT_EXPIRES_IN=24h
